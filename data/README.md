# Jarvis Dashboard Data Directory

This directory stores persistent data for the Jarvis real-time dashboard:

- `sessions.json` - Active session tracking
- `history.json` - Session history and performance data  
- `notifications.json` - System notifications
- `performance.json` - Performance metrics and analysis

The data is automatically managed by the API server.